name: Auto merge dependabot

on:
  workflow_run:
    workflows: [ 'Pull request' ]
    types: [ completed ]

jobs:
  # ALTERNATIVES
  # - GitHub App: https://kodiakhq.com/
  # - GitHub Action: https://github.com/ahmadnassri/action-dependabot-auto-merge
  dependabot-merge:
    name: 🤖️ Auto merge dependency updates
    if: github.actor == 'dependabot[bot]' && github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: 🔰 Merge
        uses: ridedott/merge-me-action@v2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PRESET: DEPENDABOT_MINOR

#      - uses: actions/checkout@v2
#      - name: 🔰️ Request a merge from dependabot
#        run: gh pr comment $PR_NUM --body "@dependabot squash and merge"
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#          PR_NUM: ${{ github.event.workflow_run.pull_requests[0].number }}
#          PR_BRANCH: ${{ github.event.workflow_run.head_branch }}
